[package]
name = "bib"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { version = "1.0.99", default-features = false }
clap = { version = "4.5.45", default-features = false, features = ["derive", "color", "debug", "std", "help"] }
